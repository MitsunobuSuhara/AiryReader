# airyPDF 開発引き継ぎ

更新日: 2026-09-13
開発場所: C:\dev\airyPDF

## 現在地
要件定義中。アプリ名・フォルダ名は airyPDF（エアリPDF）に決定。REQUIREMENTS.mdに、確定要件・未承認の仕様案・合格条件案・検証計画を整理した。公式資料に基づく技術構成の一次提案はC#＋WPF／PDFiumだが、採用は未決定。Git初期化済み（main）。要件定義書・引き継ぎ・採用アイコンを初回コミットの対象とする。コード作成、性能測定、実機印刷検証は未実施。push先は未設定。

## 目的と背景
図面を正しい原寸・指定倍率で印刷できる、軽量で広告のないPDFリーダーを作る。Adobe Acrobatは重く広告が多いと感じている。印刷プレビューが必要。最優先の性能は起動の速さと拡大縮小の滑らかさ。

## ユーザーが明示した要件
- アイコンは黒の単色背景・白い羽根の案を採用（2026-09-13）。原本を `assets/icons/airyPDF-black-source.png` に保存済み。ICO作成・外周透過と縮小表示の確認・アプリ組込みは未実施。
- 原寸・指定倍率では、はみ出しても自動縮小せず、プレビューで欠けを知らせる（2026-09-13承認）。
- 新しいPDFを開いたときの印刷倍率は100%で開始する（2026-09-13承認）。画面のズーム倍率とは別。同じPDFで印刷のたびに100%へ戻すことまでは承認されていない。
- 寸法精度はAdobe Acrobat Readerと同程度に正確な出力を目指す（2026-09-13追加）。同条件で紙上の縦横寸法を比較する検証案をREQUIREMENTS.mdに追記。数値の許容差は未定。
- 対象: 自身のWindows 11。
- プリンター: Apeos C3571。
- 主な用紙: A4・A3。
- 両面印刷。
- Nアップ: 2ページ／枚、4ページ／枚で十分。
- 小冊子印刷。
- 複数PDFをタブで開く。
- ページ回転。
- 回転した状態をPDFとして保存する。
- 範囲を選択して印刷。
- ポスター印刷（大きな図面を複数枚に分割）。
- ホイールでスクロール、Ctrl＋ホイールで拡大縮小。
- 起動の速さ、拡大縮小の滑らかさを重視。

## 会話で提示した仕様案（細部は未承認）
以下はアシスタントの提案。ユーザーがすべて明示承認したものとして扱わない。
- 新しいPDFの印刷倍率100%開始と、原寸・指定倍率で自動縮小せず欠けを知らせる仕様は上記の確定要件へ移動済み。
- PDF寸法と用紙寸法をmm表示する。
- 印刷モード: 原寸・指定倍率、用紙に合わせる、Nアップ、小冊子。加えて範囲指定・ポスター印刷。
- プリンター、用紙、向き、ページ範囲、長辺／短辺とじを指定。
- Nアップは各枠に合わせて縮小。適用倍率をプレビューで確認。
- 小冊子は左／右とじに対応。4の倍数に足りないページは末尾に空白を補う。
- 回転は左右90度、現在ページ／全ページを対象にでき、表示と印刷にも反映。
- 回転後のPDF保存は「名前を付けて保存」を標準とし、元ファイルを保護。通常の保存にはNアップや小冊子配置を反映しない。
- 範囲印刷はマウスで矩形選択し、原寸・指定倍率・用紙に合わせる印刷に対応。
- ポスター印刷は重なり幅をmm指定し、切り取り線と用紙番号を付けられる。
- プレビューで配置、余白、欠け、表裏、分割位置、枚数を確認。
- マウスポインター位置を中心に拡大縮小。
- 操作中は表示追従を優先し、停止後に細線や文字を鮮明に描画。
- 全ページの読込完了を待たずに最初のページを表示。
- ログイン・クラウド連携は不要という方向。
- 文字検索・しおり表示は、ユーザーが必要機能として選ばなかったため初版対象外と整理した。明示的な不要回答ではない。
- 設定記憶は、新しいPDFの印刷倍率100%開始のみ確定。プリンター・用紙・両面設定の記憶と同じPDF内の倍率保持期間は未確定。

## 技術・検証の未決事項
- 開発言語、UI技術、PDFエンジン、配布形式は未決定。PDFiumは候補として紹介しただけ。
- エンジン採用時に現行API、ライセンス、更新方法を確認する。
- プレビューと実印刷で同じ倍率・配置計算を利用する方針。
- プリンターから用紙、印刷可能範囲、物理余白を取得する必要がある。
- 紙上の寸法はドライバーの倍率や紙送りの誤差にも影響される。誤差ゼロを保証しない。
- 縦横100mmの基準線と代表的な図面を印刷し、実測して精度を確認する。
- 起動・ズームの数値目標は未設定。使用PCと代表PDFで測定して合格基準を決める。
- 代表PDF、PC仕様、プリンタードライバー情報は未取得。

## 次に着手すること
1. この引き継ぎと C:\dev\AGENTS.md を読む。
2. REQUIREMENTS.mdを読む。自動縮小せず欠けを知らせる方針と、新しいPDFの印刷倍率100%開始は確定済み。次は回転後のPDFを「名前を付けて保存」を標準とする案を確認する。回答された項目だけ確定へ移す。
3. 範囲印刷とポスターの併用、設定記憶、検索・しおりの扱いを順に確認する。技術構成は一次提案済みだが、採用前のライセンス・ラッパー調査と対象PC・代表PDF・ドライバーの確認は未実施。
4. 実装段階では、まずPDFを開く→原寸プレビュー→印刷で寸法精度を検証し、その後に必須機能を追加する案。試作の段階分けによって必須機能を最終要件から落とさない。

## 対応指示
日本語で対応。C:\dev\AGENTS.md がこのフォルダにも適用される。呼称はNOBUさん。変更内容は平易に説明し、区切りごとにコミットを提案。AI署名footerを付けない。Gitリポジトリは初期化済み。push先は未設定。

## 元の引き継ぎ
C:\Users\mitsu\Documents\Codex\2026-09-13\m\outputs\PDFリーダー開発_引き継ぎ.md
本資料は、その後の要件定義の会話を反映したもの。
