# airyPDF 開発引き継ぎ

更新日: 2026-09-14
開発場所: C:\dev\airyPDF


## 最新状態：デスクトップ版1.1（2026-09-14）

Adobeからの移行向けにパスワード読込、標準AcroFormの記入、欄のないPDFへの日本語追記、付箋、検索、しおり、PFX/P12署名とオフライン検証を実装。Program Filesへ管理者確認付きで登録する構成に変更。詳細と未対応はREADME、実装・検証履歴は末尾の「1.1 PDF互換機能」を参照。
利用者提示のAdobe製2ページ・80文字欄のフォームで日本語保存、権限保持、80欄と5しおりの画面読込を確認。原本・個人情報・証明書はGitと配布一式に含めない。仲間へ渡すセットアップZIPはartifacts/releaseに生成し、まだ外部送付していない。
## 正式デスクトップ版1.0（2026-09-14）

- 利用者が会社で印刷し「寸法は0.1mmくらいずれて見えるが用途では完璧」と報告。用途上合格として記録。厳密な測定条件やAcrobat比較の結果は未取得。
- デスクトップ版への格上げ依頼により1.0.0へ更新。タイトルはairyPDF 1.0、試験向けの明日の案内を普段使いのヘルプに変更。
- Windowsのユーザー別アプリ一覧とPDFの「プログラムから開く」に登録。既定アプリは変更しない。--unregisterは確認後にアプリ登録と自分のリンクのみ解除し、ファイル・設定は残す。
- 画面サイズと最大化状態をLocalAppData/airyPDF/window.jsonに記憶。印刷倍率は新PDF100%を維持。
- 以降の旧記録は実装履歴。会社での印刷未確認という旧記録は今回の利用者報告で更新。両面や小冊子は個別に確認が必要。

## 現在地

ユーザーの「続けてどんどん作って」により実装へ移行。C#・WPF・.NET 10とPDFiumによるWindows x64試作版0.2.1を作成。要件の正本はREQUIREMENTS.md、起動方法と制約はREADME.md。技術の選択や細部は実装時の初期判断であり、ユーザーが個々に明示承認したものとは区別する。

GitHub: https://github.com/MitsunobuSuhara/airyPDF （非公開、origin/main）。ユーザーは他プロジェクトと同じアカウントへの保存を確認済み。初回資料コミット5d9aa54、保存先記録e5283e7。

## 実装したこと

- PDFを複数タブで開く、ドラッグ＆ドロップ、ページ移動、幅合わせ、ホイールスクロール、Ctrl＋ホイールズーム。
- 現在／全ページを90度回転、別名保存、未保存変更の確認。
- 印刷プレビュー、原寸・指定倍率、用紙に合わせる、2／4アップ、小冊子の左右とじ・空白補充。
- 現在ページの矩形範囲印刷、ポスター分割と重なり幅。
- 用紙・向き・ページ範囲・部数・片面／両面。設定変更後はプレビュー更新まで印刷ボタンを無効にする。
- 新しいPDFの印刷倍率は100%。原寸・指定倍率では自動縮小せず欠けを知らせる。
- 印刷配置はmm単位で共通化。実印刷は画面画像を使わずPDFiumからWindowsのプリンター描画領域へ出力。
- 黒単色背景・白い羽根の採用PNGをアプリ画面に使用。原本はassets/icons/airyPDF-black-source.png。

## 確認済み・重要な修正

- Releaseビルド、PDF読込・回転保存・原本保護・ページ範囲・倍率・Nアップ・小冊子・ポスターの自動テスト。
- Microsoft Print to PDFでA4縦50%／100%、A3横200%を出力し、縦横の基準線を画像上で測定。検出した線の外形は50.25／100.25／200.25mm（4px/mmの画像測定。線幅と画素化を含むため紙上の誤差ではない）。
- PageSettings.PrintableAreaが横向き時も縦寸法を返す問題を実測で発見。CreateMeasurementGraphicsに同じPageSettingsを渡してDCから用紙・印刷可能範囲を取得する方式へ修正。
- WPF実ウィンドウの表示画像を保存して目視。印刷ボタンは左下に固定。
- 通常の実行・ファイル編集ツールとcomputer-useはsandbox helper起動エラーが出る。シェルのrequire_escalatedで実行可能。Gitは初期化時の所有者がsandboxユーザーなので、必要に応じgit -c safe.directory=C:/dev/airyPDFを使用（グローバル設定は未変更）。

## 環境・起動

- .NET SDK 10.0.401を .tools/dotnet へ導入。既存PCの.NET 8には依存しない。
- ビルド・テスト・実行用フォルダ作成: scripts/build.ps1 -Test -Publish
- 実行用: artifacts/app/airyPDF.exe（.NET同梱、フォルダ全体を保持）。
- テストPDF・画像・結果: artifacts/。SDK、ドライバー、ビルド生成物はGit対象外。
- PDFiumパッケージ155.0.8044を固定。ラッパーは使わず直接P/Invoke。関連ライセンスはTHIRD-PARTY、実行フォルダにも同梱。更新はライセンスとテストをセットで行う。

## Apeos C3571ドライバー

ユーザーの「取得して」により公式ART EXドライバー7.1.10、Windows 11 64ビット用を取得済み。
- ファイル: artifacts/drivers/ffopkplw250330w646fml.exe
- 29,382,112バイト。FUJIFILM Business Innovation Corp.の電子署名Validを確認。
- SHA256: 12F6C2105308E7B21BF191BB6FBA79905E2880E65A11950AD958E89137B9B9C4
- 公式ページ: https://www.fujifilm.com/fb/ja/support/multifunction-printers/color/download-00851
- 公式ドライバーを管理者権限で導入済み。FF Apeos C3571、キュー名「Apeos C3571 (ART EX)」。ユーザーのスクリーンショットから得たIPへTCP/IPポートを登録。導入ログはartifacts/drivers/install-result.txt。社内IPとドライバー実体はGitへ含めない。実機への通信・紙出力は未確認。

## 0.2で追加・導入したこと

- .NET同梱アプリをユーザーのLocalAppData/Programs/airyPDF/0.2.0-ハッシュへコピーし、デスクトップとスタートメニューへ登録。--installで再登録できる。元の版は削除しない。
- 採用PNGから7サイズのICOを作り、アプリとショートカットに設定。
- 「寸法確認用PDF」「使い方・登録」ボタンを追加。確認PDFはassets/checks/print-check.pdf、配布先Samples/print-check.pdf。A4縦・A4横・A3横・A4縦の4ページ、縦横100mm目盛付き。Popplerで全ページ描画して目視済み。
- プリンター名を一覧選択できるようにし、「プリンターの設定」で選んだドライバーの設定画面を開く。ネイティブ設定画面内の操作は未検証。
- Releaseビルド警告・エラー0。33件のPDF・印刷検証と、17件のUI検証に合格。Apeosを含む全3プリンター切替はプレビューのみ検証。物理プリンターには印刷ジョブを送っていない。
- 登録先から確認PDFを開いて起動。デスクトップ・スタートメニューのリンク先を確認済み。
- scripts/install-apeos.ps1は管理者実行用。初回のUACキャンセル後、ユーザーの「お願いします」で再実行し導入成功。以後、同じ導入を繰り返す必要はない。
## 連続スクロール追加（2026-09-13）

- ユーザーの依頼で全ページを縦に連続配置。スクロール位置に応じて現在ページ番号を更新し、ページ移動ボタンと番号入力も維持。
- 画面付近のみ描画し、画面外の画像は解放。見えているページ群の描画予算は合計約12MP。ページ枠は全ページ作るため極端なページ数の性能は今後確認。
- ページ境界の同時描画、最終ページ移動、ページ番号、画像解放、拡大後の位置、先頭への復帰をUIテストで追加し合格。境界の画面画像を目視。
- デスクトップ登録先も更新。起動中の古い版は未保存変更を保護するため強制終了しない。

## 0.2.1 起動先修正（2026-09-13）

- ユーザーの4ページPDFが1ページ目しか見えない報告を調査。PDF自体は4ページ。原因はタスクバーピンが連続表示追加前の0.2.0-1b04d7923fd7を起動していたこと。デスクトップだけ更新済みだった。
- インストーラーで既存のairyPDFタスクバーリンクも更新。CodexパッケージのLocalCache経由の旧インストール先も自分のリンクと判定。タイトルを0.2.1・連続スクロール対応とし、古い版と識別可能にした。
- 実際の業務PDFを読取専用で検証。ホイール入力イベントで1ページ目を越えるテストを追加し、4/4の表示画像を確認。業務PDFと画像・パスはGit対象外。AIRYPDF_UI_PDF環境変数で実ファイルの閲覧検証が可能（複数ページ限定）。印刷テストは合成PDFのまま。
- 更新後のタスクバーリンク先を確認し、対象PDFを最新版で起動済み。旧版には通常終了のみ要求。

## タスクバーの白い紙アイコンを修正（2026-09-13）

- ユーザー提供画像で白い紙アイコンを確認。リンクのTargetPathはパッケージのLocalCache実パスだがIconLocationは仮想LocalAppDataのままだった。
- GetFinalPathNameByHandleでコピー先exeの実パスを取得し、TargetPath・WorkingDirectory・IconLocationを統一。保存後にSHChangeNotifyでリンクの更新を通知。
- 実パスが同じ内容を指すことと接頭辞除去を検証。Releaseと全テスト合格。再登録後の参照先一致とexeから羽根アイコンを抽出できることを目視確認。タスクバー実表示はユーザーへ確認中。
- 公式API資料: https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-getfinalpathnamebyhandlew と https://learn.microsoft.com/en-us/windows/win32/api/shlobj_core/nf-shlobj_core-shchangenotify

## 次に着手すること

1. ユーザーはこのPCを会社へ持参する。登録済みのApeosを選び、寸法確認用PDFをA4縦・ページ1・100%で1枚出力。Acrobat Readerと同条件で紙上の縦横を実測する。次にページ3でA3横を確認。READMEに手順あり。
2. 特殊なページ領域、既存回転、フォント、画像、透明効果、フォーム、電子署名付きPDFの扱いを検証。現在はパスワード付き／512MB超を非対応として案内。
3. ポスターの切り取り線と紙上番号、アイコン外周透過整備、表示キャッシュと代表図面の性能改善を進める。
4. 設定記憶・検索・しおりは未確定。試作では未実装。原寸印刷などの必須機能を完成要件から落とさない。

紙上の寸法精度、Acrobat Readerとの同等性、実機両面・小冊子の折り順は未検証。試作の完了を製品の精度保証と扱わない。原寸自動縮小なし・新PDF100%・採用アイコン以外の細部は、ユーザーの個別承認ではなく試作の初期仕様。

## 共通指示

C:\dev\AGENTS.md適用。日本語、呼称NOBUさん。区切りでコミットを提案し、コミットした変更は承認済みoriginへpush。AI署名footerなし。1リポ1エージェント。

## 新アイコンの採用決定（2026-09-14）

ユーザーからメインへの引き継ぎ：新アイコンは比較画像の中央にあるB案を採用。
- 黒の単色背景。
- 3つの白い面で表した羽根。
- グラデーションなし。

比較画像の原本：C:/Users/mitsu/.codex/generated_images/01a09d29-d8c6-7d12-9c57-f170376b3dec/exec-91e8d48b-0aaf-4b5e-97cd-9351be59cc69.png
採用仕様は確定。現行PNG・ICOへの置き換えはこの引き継ぎでは実施していない。画像のツール表示は処理できず、中央B案の形状の目視確認は未完了。単体B案を準備し、アプリ・ショートカット・タスクバーへ反映すること。

## B案アイコン反映済み（2026-09-14）

ユーザーの「実行して」により採用B案をアプリへ反映。
- 比較画像を閲覧用に読み出し、中央B案の3面の羽根を目視確認。画像生成ツールでB案を単体化し、assets/icons/airyPDF-b.pngへ保存。黒単色背景、白い3面の羽根、グラデーションを付けない指定。
- アプリ画面・ウィンドウのPNG参照とmake-icon.ps1を新PNGへ切替。7サイズICOを再生成。従来PNGは履歴・比較用に残す。
- バージョン1.0.1へ更新。既存のデスクトップ・スタートメニュー・タスクバーリンクを更新し、実パスによるアイコン登録を維持。
- Releaseビルドと既存PDF・印刷・連続スクロールのテストに合格。新しいアイコンの反映は寸法配置処理を変更しない。

## 1.1 PDF互換機能とProgram Files構成（2026-09-14）

ユーザーはAdobeからの移行のため、パスワード、入力欄、電子署名、注釈・検索・しおりの追加を依頼。既存の入力欄と欄のないPDFへの記入の両方を希望。証明書はまだ未取得。最終的に仲間へ渡せる一式の準備も明示依頼（以前の外部配布停止方針に対する、このプロジェクトでの新しい指示）。外部送付はまだ行わない。

- バージョン1.1.0。インストール先はユーザー指定の C:\Program Files\airyPDF、配下に版とハッシュのフォルダを作る。管理者確認付き。設定はユーザー別LocalAppData。旧版とユーザーのPDFは削除しない。
- パスワード入力で暗号化PDFを開く。文字・チェック・単一選択の標準AcroFormを編集し、値と外観を保存。日本語の外観はReportLabで埋め込み、フォームをフラット化せず保持。
- 一般編集禁止でもフォーム入力が許可されたPDFはCanFillで対応。署名済み文書の編集・回転・保存は禁止して原本を保護。
- 欄のないPDFへの日本語追記、付箋作成と既存注釈一覧、しおり追加と既存しおり移動、全文字検索のページ一覧（Ctrl+Fの操作画面）。しおりの未定義移動先は無視して読み込みを続ける。
- PDFiumのフォーム専用FFLDrawを追加。フォーム情報はversion 1、JavaScript・自動外部アクションは実行しない。画面に入力欄を描画し、印刷は本文の直接描画に入力欄だけ透過画像を重ねる。
- PFX/P12証明書でPAdES署名を別名保存。pyHankoで署名値・改ざん・Windows ROOTへの信頼・変更・対象範囲を別にオフライン検証。オンライン失効確認は未実施と明示。自己署名を信頼済みと誤表示しない。
- 入力・署名処理はPyInstallerの補助exeとしてHelperへ同梱し、利用者のPythonインストールは不要。pypdf、ReportLab、pyHanko等の固定依存はsrc/pdf-helper/requirements.lock.txt。ライセンスはTHIRD-PARTY/pdf-helper。
- scripts/build-pdf-helper.ps1で補助エンジン＋機能テスト、scripts/build.ps1 -Test -Publishでアプリ、scripts/package.ps1でSetup.cmdと実行一式のZIPを作る。パッケージにはドライバー・社内IP・個人PDF・証明書・テスト結果を含めない。コード署名証明書は未導入。
- 合成PDFで日本語フォーム値・見た目、チェック、追記・注釈・しおり、暗号化保存、証明書署名、改ざん検出、署名済み編集禁止を検証。PDFium読込・検索・画面とMicrosoft Print to PDFへのフォーム印刷、従来の50/100/200%寸法テストに合格。
- 利用者提示のAdobe Acrobat Standard DC製2ページ・80文字欄の暗号化AcroFormを、このPCだけで検証。一般編集禁止・入力許可のまま日本語を検証コピーへ保存し、2ページ・80欄保持と画面での80欄・5しおり読込を確認。PDFiumとPopplerの表示を目視。原本を変更せず、本文・個人情報・検証コピーはGit/パッケージに含めない。
- 個人PDFのローカル検証用コピーはartifacts/sample-compat.pdf。AIRYPDF_TOOL_PDFで入力画面の実ファイルテスト、AIRYPDF_COMPAT_PDFでネイティブ描画と権限確認を実行できる。環境変数にはローカルパスのみ指定。

未対応をREADMEに明記：XFA、JavaScript自動計算、ラジオ・押しボタン変更、複数選択、リッチテキスト、OCR、検索ハイライト、注釈削除・他種作成、しおり編集削除、複数署名追加、ICカード、タイムスタンプ・LTV情報付加、オンラインOCSP/CRL。標準形式での互換性を確認した段階であり、Adobe全機能同等と扱わない。

1.1互換性の補足：保存時は変更した欄だけを書き換え、未変更欄の外観を保持。文字数制限とcomb（1文字ごとに枠がある入力欄）を元のフィールド構造から読み、文字数超過を拒否するテストを追加。Program Files導入先とデスクトップ・スタートメニューの参照一致を確認。タスクバーピンは現在存在しないため、存在しないピンを新規固定せず維持。

## 1.1 固定インストール先（2026-09-14）
配布先で見つけやすくするため、実行先とWindows登録を C:\Program Files\airyPDF\airyPDF.exe に固定。変更対象の既存ファイルがロック中ならコピー前に停止して閉じる案内を出す。利用者の整理依頼に基づき、最新版のハッシュ・登録・未使用・通常ディレクトリを確認して旧ハッシュ版を削除。Release、PDF/印刷自己検証、UI検証、実インストールのハッシュ一致を確認済み。最新版セットアップ: artifacts/release/airyPDF-1.1-20260914-093526.zip。

## 原本に合わせる印刷プレビューとライブ更新（2026-09-14）
利用者は向きだけでなく原本の用紙サイズに合わせる意味と訂正。表示中ページの短辺・長辺を用紙一覧と照合して初期用紙を選び、横縦も原本から選ぶ。非対応サイズは利用可能な用紙へフォールバック。設定イベントは250msのタイマーで自動更新し、変更直後は古い設定で印刷させない。手動更新ボタンは除去。A4横、A3横、倍率ライブ更新と連続入力をUIテストに追加。
検証結果: Releaseと印刷自己検証、UI検証が成功。A4横・A3横、50%自動反映、75%への連続入力を確認。このPCの固定パスのDLLが生成物と一致。最新セットアップ: artifacts/release/airyPDF-1.1-20260914-094406.zip。GitHub送信は自動承認審査で今回分の明示承認不足として拒否。ローカル保存済み、push待ち。

## 閲覧ツールバー整理（2026-09-14）
利用者の画面指摘に対応。表示倍率を入力欄にしてEnter／フォーカス移動で確定、100％復帰ボタンを追加。倍率は10～800％、不正値は元に戻す。画面幅に合わせるへ改称。回転は矢印アイコン、全ページ適用はその他メニューへ。タブを閉じる（Ctrl+W）、寸法確認PDF、使い方もその他へ。範囲選択の閲覧実装を削除、印刷側の範囲チェックも非表示。上段は開く・印刷・保存・編集／検索・その他に整理。
検証: Release、PDF/印刷自己検証、UI検証が成功。倍率100・125%入力、NaN拒否、100%復帰、範囲選択削除を確認。実ウィンドウ画像を目視し、このPCのインストールDLL一致を確認。GitHub送信は前回分から承認待ち。

## 倍率操作の調整（2026-09-14）
利用者の追加指示で100％ボタンを削除、画面幅に合わせるを操作バー右端へ移動。Ctrl＋ホイールは100％をまたぐ1回を原寸で止め、次の操作では先へ進む。拡大縮小の両方向と100％から先へ進めることをUI検証に追加。
検証: Release、PDF/印刷、UI検証が成功。100％停止の上下両方向、停止後の拡大縮小継続、右端配置、100％ボタン削除を確認。浮動小数の原寸付近は正確に1へ補正。このPCのDLLと生成物一致。
